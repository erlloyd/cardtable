name: Build and Deploy
on:
  push:
    branches: [main]
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v2.3.1 # If you're using actions/checkout@v2 you must set persist-credentials to false in most cases for the deployment to work correctly.
        with:
          persist-credentials: false

      - name: Unzip Card Images
        uses: montudor/action-zip@v0.1.0
        with:
          args: unzip -qq .imagepacks/mc/core.zip

      - name: Unzip Card Images
        uses: montudor/action-zip@v0.1.0
        with:
          args: unzip -qq .imagepacks/mc/Captain_America_Image_Pack.zip

      - name: Unzip Card Images
        uses: montudor/action-zip@v0.1.0
        with:
          args: unzip -qq .imagepacks/mc/Ronan_Image_Pack.zip

      - name: Unzip Card Images
        uses: montudor/action-zip@v0.1.0
        with:
          args: unzip -qq .imagepacks/mc/Black_Widow_Image_Pack.zip

      - name: Unzip Card Images
        uses: montudor/action-zip@v0.1.0
        with:
          args: unzip -qq .imagepacks/mc/Hulk_Image_Pack.zip

      - name: Unzip Card Images
        uses: montudor/action-zip@v0.1.0
        with:
          args: unzip -qq .imagepacks/mc/Ms_Marvel_Image_Pack.zip

      - name: Unzip Card Images
        uses: montudor/action-zip@v0.1.0
        with:
          args: unzip -qq .imagepacks/mc/Thor_Image_Pack.zip

      - name: Unzip Card Images
        uses: montudor/action-zip@v0.1.0
        with:
          args: unzip -qq .imagepacks/mc/Ant_Man_Image_Pack.zip

      - name: Unzip Card Images
        uses: montudor/action-zip@v0.1.0
        with:
          args: unzip -qq .imagepacks/mc/Doctor_Strange_Image_Pack.zip

      - name: Unzip Card Images
        uses: montudor/action-zip@v0.1.0
        with:
          args: unzip -qq .imagepacks/mc/Green_Goblin_Image_Pack.zip

      - name: Unzip Card Images
        uses: montudor/action-zip@v0.1.0
        with:
          args: unzip -qq .imagepacks/mc/Wrecking Crew Image Pack.zip

      - name: Unzip Card Images
        uses: montudor/action-zip@v0.1.0
        with:
          args: unzip -qq .imagepacks/mc/Red_Skull_Image_Pack.zip

      - name: Unzip Card Images
        uses: montudor/action-zip@v0.1.0
        with:
          args: unzip -qq .imagepacks/mc/Baron_Zemo_Firestarter_Image_Pack.zip

      - name: Install
        run: yarn install

      - name: Sanity check file got created
        run: cat src/external/generated/packs.ts

      - name: Move Card Images
        run: sudo mv 055c536f-adba-4bc2-acbf-9aefb9756046/Sets/dea583bf-554b-43c8-a6ed-a44782094ad1/Cards/* public/images/cards

      - name: Build 🔧
        run: yarn build:prod

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@3.6.2
        with:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          BRANCH: gh-pages # The branch the action should deploy to.
          FOLDER: build # The folder the action should deploy.
          CLEAN: true # Automatically remove deleted files from the deploy branch
